name: 🟩 Backend (Node.js) CI/CD

on:
  push:
    branches: [main, develop]
    paths:
      - "apps/backend/**"
      - ".github/workflows/backend-node.yml"
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - "apps/backend/**"
      - ".github/workflows/backend-node.yml"

permissions:
  contents: read

concurrency:
  group: backend-node-${{ github.ref }}
  cancel-in-progress: true

env:
  APP_DIR: apps/backend
  DOCKER_IMAGE: ghcr.io/${{ github.repository }}-backend-node
  TRIVY_SEVERITY: CRITICAL,HIGH,MEDIUM

jobs:
  validate:
    name: 🔍 Lint, Test & Build
    runs-on: ubuntu-latest
    timeout-minutes: 18
    defaults:
      run:
        working-directory: ${{ env.APP_DIR }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🛡️ Secrets Scan (Gitleaks)
        uses: gitleaks/gitleaks-action@v2
        with:
          args: detect --verbose --redact --exit-code 1 --config=.gitleaks.toml

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: ${{ env.APP_DIR }}/package-lock.json

      - name: 📦 Install
        run: |
          set -euo pipefail
          npm ci --fund=false

      - name: 🧹 Lint
        run: npm run lint --if-present

      - name: 🧪 Tests
        run: npm test -- --watchAll=false --ci

      - name: 🏗️ Build
        run: npm run build --if-present

  docker:
    name: 🐳 Build, Scan & Push (main only)
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 30
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: read
      packages: write
      security-events: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔎 Detect Dockerfile
        id: docker
        run: |
          if [ -f "${{ env.APP_DIR }}/Dockerfile" ]; then
            echo "has_docker=true" >> $GITHUB_OUTPUT
          else
            echo "has_docker=false" >> $GITHUB_OUTPUT
          fi

      - name: 🧰 Set up Docker Buildx
        if: steps.docker.outputs.has_docker == 'true'
        uses: docker/setup-buildx-action@v3

      - name: 📤 Login to GHCR
        if: steps.docker.outputs.has_docker == 'true'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🏗️ Build & Push
        if: steps.docker.outputs.has_docker == 'true'
        uses: docker/build-push-action@v6
        with:
          context: ${{ env.APP_DIR }}
          push: true
          tags: |
            ${{ env.DOCKER_IMAGE }}:${{ github.sha }}
            ${{ env.DOCKER_IMAGE }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: true
          sbom: false

      - name: 🛡️ Trivy image scan (SARIF)
        if: steps.docker.outputs.has_docker == 'true'
        uses: aquasecurity/trivy-action@0.21.0
        with:
          image-ref: ${{ env.DOCKER_IMAGE }}:${{ github.sha }}
          format: sarif
          output: trivy-backend-node.sarif
          severity: ${{ env.TRIVY_SEVERITY }}
          ignore-unfixed: true
          exit-code: 1

      - name: ⚠️ Upload SARIF
        if: steps.docker.outputs.has_docker == 'true'
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-backend-node.sarif

      - name: 📦 SBOM (SPDX)
        if: steps.docker.outputs.has_docker == 'true'
        uses: anchore/sbom-action@v0
        with:
          image: ${{ env.DOCKER_IMAGE }}:${{ github.sha }}
          output-file: sbom-backend-node.spdx.json

      - name: 📤 Upload SBOM artifact
        if: steps.docker.outputs.has_docker == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: sbom-backend-node-${{ github.sha }}
          path: sbom-backend-node.spdx.json