# =========================
# frontend-next.yml
# =========================
name: 🟨 Frontend (Next.js) CI/CD

on:
  push:
    branches: [main, develop]
    paths:
      - "apps/frontend/**"
      - ".github/workflows/frontend-next.yml"
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - "apps/frontend/**"
      - ".github/workflows/frontend-next.yml"

permissions:
  contents: read

concurrency:
  group: frontend-next-${{ github.ref }}
  cancel-in-progress: true

env:
  APP_DIR: apps/frontend
  DOCKER_IMAGE: ghcr.io/${{ github.repository }}-frontend
  TRIVY_SEVERITY: CRITICAL,HIGH,MEDIUM

jobs:
  validate:
    name: 🔍 Lint & Test
    runs-on: ubuntu-latest
    timeout-minutes: 12
    defaults:
      run:
        working-directory: ${{ env.APP_DIR }}
    steps:
      - uses: actions/checkout@v4

      - name: 🛡️ Secrets Scan (Gitleaks)
        uses: gitleaks/gitleaks-action@v2
        with:
          args: detect --verbose --redact --exit-code 1
          config-path: .gitleaks.toml

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: ${{ env.APP_DIR }}/package-lock.json

      - name: 📦 Install
        run: npm ci --audit --fund=false

      - name: 🧹 Lint
        run: npm run lint --if-present

      - name: 🧪 Tests
        run: npm test -- --watchAll=false --ci || true

      - name: 🏗️ Build
        run: npm run build

  docker:
    name: 🐳 Build & Push (main only)
    runs-on: ubuntu-latest
    needs: validate
    permissions:
      contents: read
      packages: write
      security-events: write
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: 🔎 Detect Dockerfile
        id: docker
        run: |
          if [ -f "${{ env.APP_DIR }}/Dockerfile" ]; then echo "has_docker=true" >> $GITHUB_OUTPUT; else echo "has_docker=false" >> $GITHUB_OUTPUT; fi

      - name: 🧰 Set up Docker Buildx
        if: steps.docker.outputs.has_docker == 'true'
        uses: docker/setup-buildx-action@v3

      - name: 📤 Login to GHCR
        if: steps.docker.outputs.has_docker == 'true'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🏗️ Build & Push
        if: steps.docker.outputs.has_docker == 'true'
        uses: docker/build-push-action@v6
        with:
          context: ${{ env.APP_DIR }}
          push: true
          tags: |
            ${{ env.DOCKER_IMAGE }}:${{ github.sha }}
            ${{ env.DOCKER_IMAGE }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: 🛡️ Trivy image scan (SARIF)
        if: steps.docker.outputs.has_docker == 'true'
        uses: aquasecurity/trivy-action@0.21.0
        with:
          image-ref: ${{ env.DOCKER_IMAGE }}:${{ github.sha }}
          format: sarif
          output: trivy-frontend.sarif
          severity: ${{ env.TRIVY_SEVERITY }}
          ignore-unfixed: true
          exit-code: 1

      - name: ⚠️ Upload SARIF
        if: steps.docker.outputs.has_docker == 'true'
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-frontend.sarif

      - name: 📦 SBOM (SPDX)
        if: steps.docker.outputs.has_docker == 'true'
        uses: anchore/sbom-action@v0
        with:
          image: ${{ env.DOCKER_IMAGE }}:${{ github.sha }}
          output-file: sbom-frontend.spdx.json

      - name: 📤 Upload SBOM artifact
        if: steps.docker.outputs.has_docker == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: sbom-frontend-${{ github.sha }}
          path: sbom-frontend.spdx.json