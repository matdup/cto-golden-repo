# Contracts (Source of Truth)

This directory defines API contracts and shared schemas.

- openapi/: HTTP API contracts
- schemas/: shared DTOs/events/errors
- compat/: compatibility rules (breaking/non-breaking)
- contract-tests/: consumer/provider contract tests (Pact)