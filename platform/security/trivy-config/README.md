# Trivy Configuration

Place Trivy config here:
- policy / ignore rules (if justified)
- scanners selection
- severity thresholds

Rules:
- Never ignore CRITICAL without written justification
- Any ignore must reference ticket + expiry date