# Monitoring Stack (local defaults)
# Never commit real secrets. Use .env locally and GitHub Secrets in CI.

# Grafana
GRAFANA_ADMIN_PASSWORD=change_this_password
GRAFANA_SECRET_KEY=change_me_32chars_min

# Prometheus
PROMETHEUS_RETENTION=30d
PROMETHEUS_ENABLE_LIFECYCLE=false
PROMETHEUS_ENABLE_ADMIN_API=false

# Loki (note: filesystem mode by default)
LOKI_RETENTION_PERIOD=744h

# Alerting
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ
ALERTS_SLACK_CHANNEL=#alerts

# External URLs (optional, used behind reverse-proxy)
DOMAIN_NAME=monitoring.example.com
ALERTMANAGER_EXTERNAL_URL=https://alerts.example.com

# Vault (dev profile only)
VAULT_DEV_ROOT_TOKEN_ID=root-token
VAULT_ADDR=http://localhost:8200